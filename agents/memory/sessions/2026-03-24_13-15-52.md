# Session Log - 2026-03-24 13:15:52

## Objective
Add Ollama Cloud support by adding a `base_url` column to `ProviderConfigs` and making endpoints dynamic.

## Plan
1. Create Migration `008_add_base_url_to_providers.sql`.
2. Update `listModels()` in `aiService.js` to read dynamic URL from DB.
3. Update API execution functions inside `aiService.js` to call `active.base_url` for Ollama providers.
4. Verify with scripts.
