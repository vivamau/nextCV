# Session Log - 2026-03-24 11:24:35

## Objective
Track token usage (in / out) across operations in a database table.

## Plan
1. Create database migration `007_token_usage_table.sql` with `started_at` & `ended_at`.
2. Add `logTokenUsage` helper in `aiService.js`.
3. Update each wrapper (`parseJobListings`, etc.) to extract token limits and log them.
4. Verify with test script and query checks.
