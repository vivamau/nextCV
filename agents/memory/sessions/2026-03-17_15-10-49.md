# Session Log - 2026-03-17 15:10:49

## Objective
Add a `skills` field to the `Resumes` table, extracted by AI and augmentable by user interface.

## Plan
1. Create a migration script to add `skills` text field to `Resumes` table or `CVs` (Resumes in DB).
2. Update AI parsing logic to extract skills.
3. Update API endpoints to support skills updates.
4. Build UI for managing skills in CV view.
5. Verify with TDD.
